import React from 'react';

const TheHeader = () => {
    return (
        <header className='site-header sticky-top'>
            <div className='container-fluid'>I am header</div>
        </header>
    );
};

export default TheHeader;
